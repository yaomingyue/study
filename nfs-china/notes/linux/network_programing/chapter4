1 tcp并发服务器实现

1 父进程调用accept之后调用fork

怎样处理回收子进程资源


2 多线程和多进程select的选择

简单,易控是进程的优点,运行资源占用大是其弱点,适合于小容量服务器.
select不可抢占,对单个用户响应快,但对全部用户而言则响应迟缓
线程具有运行资源少的优点,也有进程响应时间短的优点,但控制复杂是其弱点,适合于高级程序员
大容量的服务器一般采用线程和select相结合的方式来处理.

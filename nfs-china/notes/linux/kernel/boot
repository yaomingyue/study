/boot 目录作用
1 config-2.6...    当前内核的配置文件，可以用它来编译内核。

2 initrd...       初始化ramdisk，里边放的是启动时要加载的驱动。
    系统启动是要很多外设的帮助，cpu，内存等的驱动在kernel中，硬盘，usb初始化在initrd中，

3 vmlinuz  这是真正的kernel。

4 system.map   各种符号（变量和函数）的地址，
每次编译内核需要替换，据说不替换也能启动，好像是供外部程序使用，了解kernel
运行过程

